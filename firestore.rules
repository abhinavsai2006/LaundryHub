rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for development/demo purposes
    // TODO: Update with proper security rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}